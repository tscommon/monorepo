import CodeBlock from '@theme/CodeBlock';
import usage from '!!raw-loader!../../../box/examples/usage.ts';

# Box

Creates a boxed value using an object (a box) and then places x into it.

<CodeBlock language="ts" title="Usage">
  {usage.replace('../src', '@tscommon/box')}
</CodeBlock>
